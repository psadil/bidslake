# ADR NNNN — Short imperative title

```
ADR: NNNN
Title: Short imperative title
Status: Provisional
Type: Design
Created: DD-MMM-YYYY
Requires: NNNN
```

<!--
Copy this file to `NNNN-slug.md` and delete these comments as you fill each section.

Three rules the whole set follows:

1. Write it as a description of how the system works NOW. No dated amendments, no
   "superseded by", no "this used to...", no commit SHAs, no tracker ids. When a decision
   changes, rewrite the record — git is the history.
2. Motivation is why the existing state is inadequate, including why the mechanisms that
   already exist do not cover the case. Rationale is why THIS design rather than another,
   with the evidence. Putting an argument about an existing mechanism's insufficiency into
   Rationale is the most common mistake.
3. A measurement is evidence and stays, but never bare: say what was measured and on what,
   so a reader can re-run it.

Do NOT add a "Reference Implementation" section. PEP has one because a PEP may describe something
not yet built; these records describe a working system, so the whole document is about the
implementation. An inventory of file paths and function names is a list nothing verifies, stale at
the first rename. Name a symbol inline, in the sentence that needs it, or not at all.

Omit a section entirely rather than writing "None." Drop `Requires:` if nothing applies.
-->

## Abstract

<!-- ~200 words. What the decision is, stated so someone who reads only this knows the shape. -->

## Motivation

<!-- Why the current state is inadequate. What breaks, or cannot be expressed, without this. -->

## Rationale

<!--
Why this design and not another, and the evidence. Mirror the Specification's structure with
`### On <topic>` subsections where that helps. A heading that restates itself
("### Why X is not Y" followed by a paragraph saying X is not Y) means the point belongs in
Motivation as one sentence.
-->

## Specification

<!-- Normative only: what the design IS. The argument lives in Rationale. -->

### 1. Short claim

### 2. Short claim

## Backwards Compatibility

<!-- What breaks, what must migrate, what existing catalogs must do. "Nothing" is a fine answer. -->

## Rejected Ideas

<!--
Every alternative seriously considered, with why it lost. This section is load-bearing — it is
how a future reader finds out their idea has already been tried. Name the option in bold, then
the reason.
-->

**The rejected option.** Why it lost.

## Open Issues

<!-- Genuinely unresolved design questions. Delete the section if there are none. -->
