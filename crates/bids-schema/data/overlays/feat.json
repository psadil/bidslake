{
  "objects": {
    "entities": {
      "from": {
        "name": "from",
        "display_name": "From",
        "description": "The source space or reference of a spatial transform (e.g. `from-T1w`). Only applicable to derivative data.\n",
        "type": "string",
        "format": "label"
      },
      "to": {
        "name": "to",
        "display_name": "To",
        "description": "The target space or reference of a spatial transform (e.g. `to-MNI152NLin2009cAsym`). Only applicable to derivative data.\n",
        "type": "string",
        "format": "label"
      },
      "mode": {
        "name": "mode",
        "display_name": "Mode",
        "description": "The interpretation of a transform file (e.g. `mode-image`). Only applicable to derivative data.\n",
        "type": "string",
        "format": "label"
      }
    },
    "suffixes": {
      "timeseries": {
        "value": "timeseries",
        "display_name": "Time series",
        "description": "A tabular file whose rows are ordered samples in register with an associated time series (e.g. fMRIPrep confounds, one row per functional volume).\n"
      },
      "xfm": {
        "value": "xfm",
        "display_name": "Transformation",
        "description": "A spatial transform mapping one space to another, typically named with `from-`, `to-`, and `mode-` entities.\n"
      },
      "boldref": {
        "value": "boldref",
        "display_name": "BOLD reference image",
        "description": "A single 3D volume used as the reference for a BOLD run.\n"
      },
      "mixing": {
        "value": "mixing",
        "display_name": "Mixing matrix",
        "description": "The mixing matrix of a decomposition: one column per component, one row per volume of the decomposed time series.\n"
      },
      "components": {
        "value": "components",
        "display_name": "Component maps",
        "description": "The spatial maps of a decomposition, concatenated along a component axis.\n"
      },
      "classification": {
        "value": "classification",
        "display_name": "Component classification",
        "description": "A labelling of a decomposition's components (e.g. signal versus noise), produced automatically or by a rater.\n"
      },
      "spectrum": {
        "value": "spectrum",
        "display_name": "Component power spectra",
        "description": "The power spectra of a decomposition's components: one column per component, one row per frequency bin.\n"
      },
      "metrics": {
        "value": "metrics",
        "display_name": "Component metrics",
        "description": "Scalar metrics describing a decomposition's components, one row per component (e.g. MELODIC's explained-variance table, FIX's classifier features).\n"
      }
    },
    "extensions": {
      "mat": {
        "value": ".mat",
        "display_name": "FSL affine transform",
        "description": "A plain-text 4x4 affine matrix in FSL's convention, as written by `flirt` and `convert_xfm`.\n"
      },
      "par": {
        "value": ".par",
        "display_name": "Motion parameters",
        "description": "A whitespace-delimited, headerless table of rigid-body motion parameters, one row per volume, as written by `mcflirt`.\n"
      }
    },
    "columns": {
      "trans_x__confounds": {
        "name": "trans_x",
        "display_name": "Translation (x)",
        "description": "Estimated framewise translation along the x axis (mm).\n",
        "type": "number"
      },
      "trans_y__confounds": {
        "name": "trans_y",
        "display_name": "Translation (y)",
        "description": "Estimated framewise translation along the y axis (mm).\n",
        "type": "number"
      },
      "trans_z__confounds": {
        "name": "trans_z",
        "display_name": "Translation (z)",
        "description": "Estimated framewise translation along the z axis (mm).\n",
        "type": "number"
      },
      "rot_x__confounds": {
        "name": "rot_x",
        "display_name": "Rotation (x)",
        "description": "Estimated framewise rotation about the x axis (radians).\n",
        "type": "number"
      },
      "rot_y__confounds": {
        "name": "rot_y",
        "display_name": "Rotation (y)",
        "description": "Estimated framewise rotation about the y axis (radians).\n",
        "type": "number"
      },
      "rot_z__confounds": {
        "name": "rot_z",
        "display_name": "Rotation (z)",
        "description": "Estimated framewise rotation about the z axis (radians).\n",
        "type": "number"
      },
      "explained_variance__icstats": {
        "name": "explained_variance",
        "display_name": "Explained variance (%)",
        "description": "The component's share of the variance the decomposition retained, as a percentage (MELODIC's report calls this '% of explained variance').\n",
        "type": "number"
      },
      "total_variance__icstats": {
        "name": "total_variance",
        "display_name": "Total variance (%)",
        "description": "The component's share of the total data variance: explained variance scaled by the fraction of variance the PCA subspace kept (MELODIC's report calls this '% of total variance').\n",
        "type": "number"
      },
      "signal_change_pos__icstats": {
        "name": "signal_change_pos",
        "display_name": "Signal change at positive peak (%)",
        "description": "Percent signal change of the fitted component at the voxel where its spatial map is maximal (MELODIC's report calls this '% signal change (pos peak voxel)'). MELODIC writes this pair only when the data are in memory, so a two-column file leaves it NULL.\n",
        "type": "number"
      },
      "signal_change_neg__icstats": {
        "name": "signal_change_neg",
        "display_name": "Signal change at negative peak (%)",
        "description": "Percent signal change of the fitted component at the voxel where its spatial map is minimal, negated (MELODIC's report calls this '% signal change (peak neg. voxel)').\n",
        "type": "number"
      }
    }
  },
  "rules": {
    "tabular_data": {
      "feat": {
        "feat_motion": {
          "selectors": [
            "datatype == \"func\"",
            "suffix == \"timeseries\"",
            "extension == \".par\""
          ],
          "additional_columns": "not_allowed",
          "initial_columns": [
            "rot_x__confounds",
            "rot_y__confounds",
            "rot_z__confounds",
            "trans_x__confounds",
            "trans_y__confounds",
            "trans_z__confounds"
          ],
          "columns": {
            "rot_x__confounds": "required",
            "rot_y__confounds": "required",
            "rot_z__confounds": "required",
            "trans_x__confounds": "required",
            "trans_y__confounds": "required",
            "trans_z__confounds": "required"
          }
        },
        "feat_icstats": {
          "selectors": [
            "datatype == \"func\"",
            "suffix == \"metrics\"",
            "extension == null"
          ],
          "additional_columns": "not_allowed",
          "initial_columns": [
            "explained_variance__icstats",
            "total_variance__icstats",
            "signal_change_pos__icstats",
            "signal_change_neg__icstats"
          ],
          "columns": {
            "explained_variance__icstats": "required",
            "total_variance__icstats": "required",
            "signal_change_pos__icstats": "optional",
            "signal_change_neg__icstats": "optional"
          }
        }
      }
    }
  }
}
